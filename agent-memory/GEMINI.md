# Project Context
A Jetski plugin for agent memory management, consisting of skills, subagents, and helper scripts.

# Operational Commands
## Verification
bash scripts/verify-memory-jetski.sh

# Style & Conventions
- No specific style preferences defined beyond standard language conventions.

# Architecture & Constraints
- Follows Jetski plugin structure with `skills/`, `agents/`, and `scripts/`.
- `plugin.json` defines the plugin name.
- `hooks.json` defines Jetski hooks.
