# TODO

## P1 — Important / Unblocking

- [ ] **[P1]** Audit existing Python scripts — Ensure that error handling and type hints are comprehensively used across `scripts/*.py`.
- [ ] **[P1]** Verify ADC fallback — Confirm that authentication degrades gracefully if Application Default Credentials are not available or are misconfigured.

## P2 — Nice-to-Have

- [ ] **[P2]** Add unit tests — Implement baseline unit tests under the `tests/` directory to validate memory loading and updating logic.
